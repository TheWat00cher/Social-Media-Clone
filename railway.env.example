# Railway Environment Variables
# Copy these to Railway Variables tab

NODE_ENV=production
PORT=5000

# MongoDB Atlas - Get from: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/socialconnect?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret - Generate a random 32+ character string
# Example: openssl rand -base64 32  (run in terminal)
JWT_SECRET=your_super_secret_random_32_plus_character_string_here

# JWT Expiration
JWT_EXPIRE=7d

# Cloudinary - Get from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# CORS - Update this after deploying frontend to Vercel
CORS_ORIGIN=http://localhost:3000

# Optional: Client URL (same as CORS_ORIGIN)
CLIENT_URL=http://localhost:3000
